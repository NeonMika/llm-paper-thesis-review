{
  "name": "app",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "prod-binary": "bun build --compile --minify-whitespace --minify-syntax --target bun --outfile build/server ./src/index.ts",
    "prod": "bun build --minify-whitespace --minify-syntax --target bun --outfile build/server ./src/index.ts"
  },
  "dependencies": {
    "@ai-sdk/google": "^2.0.2",
    "@elysiajs/cors": "^1.3.3",
    "@elysiajs/swagger": "^1.3.1",
    "ai": "^5.0.5",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
